"""Taskboard + Traces (real data via main app or direct)."""
import streamlit as st

st.header("Taskboard & Live Traces")
st.write("See main dashboard/app.py Taskboard & Traces page for dataframe, filters, expanders and live refresh.")
st.caption("Data loaded via mana_agent.ui.streamlit_helpers from real TaskBoard / TraceWriter artifacts.")
